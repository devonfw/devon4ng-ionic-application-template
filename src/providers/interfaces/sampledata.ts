/** The sampledata class from the server. */
export interface Sampledata {
    id?: number;
    modificationCounter?: number;
    revision?: number;
    name:string,
    surname:string,
    age:number,
    mail:string,
}